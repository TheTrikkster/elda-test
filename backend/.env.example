# Backend — copy to .env
DATABASE_URL=postgresql://elda:elda@localhost:5432/elda
PORT=3000
# Comma-separated origins allowed for CORS / WebSocket
CORS_ORIGIN=http://localhost:5173
# Simulator tick interval in ms (new position pushed per machine)
SIMULATOR_INTERVAL_MS=1500
