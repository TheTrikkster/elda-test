# Frontend — copy to .env
# Public Mapbox token (pk....). Create a free one at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=
# Backend REST + WebSocket base URL
VITE_API_URL=http://localhost:3000
